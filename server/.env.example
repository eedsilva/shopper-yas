# Connection string for Prisma. Use SQLite by default.
DATABASE_URL="file:./dev.db"
# Force Prisma to use the WASM query engine so no native binary download is required.
PRISMA_CLIENT_ENGINE_TYPE="wasm"
